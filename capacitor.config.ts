import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ecurie-de-persevere-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
